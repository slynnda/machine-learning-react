var Config = require('./Config')

function moveBall(ball, paddle1, paddle2) {
    // not touching anything
    var newX = ball.pos.x + ball.vel.x
    var newY = ball.pos.y + ball.vel.y

    // need logic for colliders


}

